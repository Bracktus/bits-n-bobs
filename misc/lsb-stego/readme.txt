Basic least bit steganography.

Only works with truecolour PNGs with an alpha channel.

Usage:
- To encode `python3 lsb.py --encode --image image.png --message "Secret Message" --output out.png`
- To decode `python3 lsb.py --decode --image image.png` 


